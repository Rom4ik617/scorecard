  <div class="container">
    <h1 class="header">🔥 Top Contributors for the Current Week 🔥</h1>

    <% if @weekly_top_contributors.any? %>
      <ol class="list">
        <% @weekly_top_contributors.each do |week, data| %>
          <li class="list-item">
            <strong>Week starting <%= week %>:</strong>
            <p class="project-info">Project: <%= @repository %></p>
            <ul class="sub-list">
              <% data[:event_points].sort_by { |_contributor, points| points }.reverse.first(3).each_with_index do |(contributor, points), index| %>
                <li class="sub-list-item">
                  <%= case index
                       when 0 then '🥇'
                       when 1 then '🥈'
                       when 2 then '🥉'
                     end %>
                  <%= contributor %> - <%= points %> points
                </li>
              <% end %>
            </ul>
          </li>
        <% end %>
      </ol>
    <% else %>
      <p class="no-events">No events found for the current week.</p>
    <% end %>
  </div>